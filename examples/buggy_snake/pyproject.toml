[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "buggy-snake"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
buggy-snake = "terminal_snake.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "-q"
